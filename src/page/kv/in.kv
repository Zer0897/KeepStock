#:kivy 1.10.1

<InPage>:
    name: 'in'
    title: self.name.title()
    SecondaryView:
        id: view
        title: root.title
    # AnchorLayout:
    #     Scanner:
    #         id: scanner

    AnchorLayout:
        PrimaryButton:
            text: "Toggle"
            size_hint: .4, .4
            on_press:
                scanner.play = not scanner.play

